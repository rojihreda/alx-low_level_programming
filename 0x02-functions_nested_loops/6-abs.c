#include"main.h"
#include<stdlib.h>

/**
 * _abs - A function that makes a negative number positive
 *
 * @x: the num to into positive
 *
 *
 * Return: return 'x'
 */

int _abs(int x)
{
	return (abs(x));
}
